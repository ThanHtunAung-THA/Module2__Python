import os

open('8_file and folder/testing.txt','a')


'''
    'r': Read mode. Opens the file for reading (default mode). The file must exist.
    'w': Write mode. Opens the file for writing. Creates a new file if it does not exist or truncates the file if it exists.
    'a': Append mode. Opens the file for writing. Creates a new file if it does not exist, and appends to the file if it exists.
    'x': Exclusive creation mode. Creates a new file, but fails if the file already exists.
    'b': Binary mode. Used for binary files (e.g., images, executable files).
    't': Text mode. Used for text files (default mode).
    eg::
    The open('file.txt', 'x') command is used to create a new file
'''
